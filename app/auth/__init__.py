"""
=============================================================================
קובץ: auth/__init__.py
שייך ל: צד שרת (Server-Side)
=============================================================================
תפקיד הקובץ:
    קובץ __init__.py הופך את התיקייה auth/ לחבילה (package) של Python.
    בלי קובץ זה, Python לא יוכל לייבא מודולים מתוך התיקייה.

    התיקייה auth/ מכילה את כל הקוד שקשור לאימות משתמשים:
    - routes.py    → ראוטים (endpoints) להתחברות, הרשמה, שכחתי סיסמה
    - validation.py → ולידציית חוזק סיסמה
=============================================================================
"""

"""מודול אימות."""
